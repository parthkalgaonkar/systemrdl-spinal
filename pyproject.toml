[project]
name = "systemrdl-spinal"
version = "0.1.0"
description = "Utility for generate spinalHDL wrapper from peakrdl"
authors = [ ]
requires-python = ">=3.11"
dependencies = [
    "peakrdl",
    "peakrdl-regblock",
    "lark",
]
